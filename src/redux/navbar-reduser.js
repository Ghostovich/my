let initialState = {
    Friends: [
        {id: 1, name: "Phil"},
        {id: 2, name: "Anton"},
        {id: 3, name: "Vasya"},
    ]
};

let sitebarReducer = (state=initialState,action) => {
return state
}

export default sitebarReducer